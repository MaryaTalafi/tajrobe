const fs = require('fs'); const path = require('path'); const walk = (dir) = let results = []; const list = fs.readdirSync(dir); list.forEach((file) = file = dir + '/' + file; const stat = fs.statSync(file); if (stat 
